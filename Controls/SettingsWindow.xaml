<Window x:Class="Visunovia.Controls.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置 - 引擎配置"
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1A1A1A">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" Text="引擎设置"
                   FontSize="16" FontWeight="Bold"
                   Foreground="White" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="LanguageLabel" Grid.Column="0" Text="语言 / Language:"
                       Foreground="White" VerticalAlignment="Center"/>

            <ComboBox x:Name="LanguageComboBox" Grid.Column="1"
                      Height="28" VerticalAlignment="Center"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="取消" Width="80" Height="32"
                    Margin="0,0,10,0"
                    Background="#3D3D3D" Foreground="White"
                    BorderThickness="0"
                    Click="CancelButton_Click"/>
            <Button x:Name="OkButton" Content="确定" Width="80" Height="32"
                    Background="#3B82F6" Foreground="White"
                    BorderThickness="0"
                    Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
